# Example environment variables for Docker deployment with self-hosted PostgreSQL
# Copy this to .env and fill in your values

# Database Configuration (for local PostgreSQL in Docker)
DB_USER=swiftie
DB_HOST=db
DB_NAME=SwiftieSanctuary
DB_PASSWORD=your_secure_password_here
DB_PORT=5432

# Server Configuration
PORT=3000
NODE_ENV=production
SESSION_SECRET=your-long-random-secret-key-here
